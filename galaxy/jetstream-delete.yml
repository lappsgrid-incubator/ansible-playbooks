
- hosts: localhost
  vars_files:
    vars/jetstream.yml
  tasks:
    - name: Setup
      include_vars: vars/jetstream.yml
    - name: Delete Jetstream instance(s)
      os_server:
        state: absent
        name: "{{ item.name }}"
        image: "{{ item.image }}"
      loop: "{{ servers }}"

