---
galaxy:
  port: 8000
  user: galaxy
  root: /home/galaxy
  branch: "{{ galaxy_branch }}"
  home: /home/galaxy/galaxy
  repo: https://github.com/lappsgrid-incubator/Galaxy.git

mods:
  branch: "{{ mods_branch }}"
  home: /home/galaxy/mods
  repo: https://github.com/lappsgrid-incubator/GalaxyMods.git


# The location of the LAPPS Grid certificates and private key.  These must be loaded from a secure
# location on your local machine.
# DO NOT CHECK THE PRIVATE KEY INTO SOURCE CONTROL!
certificate_path: /Users/suderman/certs/2018
